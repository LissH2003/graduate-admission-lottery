# 数据库迁移文件分析报告

**分析时间**: 2026-03-11  
**分析范围**: supabase/migrations/*.sql

---

## 一、迁移文件列表

| 序号 | 文件名 | 用途 | 状态 |
|------|--------|------|------|
| 001 | create_tables.sql | 创建核心表结构 | ✅ 必要 |
| 002 | create_indexes.sql | 创建索引 | ✅ 必要 |
| 003 | create_functions.sql | 创建基础函数 | ⚠️ 部分无用 |
| 004 | enable_rls.sql | 启用行级安全 | ✅ 必要 |
| 005 | seed_data.sql | 初始数据 | ✅ 必要 |
| 006 | add_sso_support.sql | SSO支持 | ⚠️ 部分无用 |
| 007 | add_verify_user_rpc.sql | 统一登录验证 | ✅ 必要 |
| 008 | cleanup_legacy.sql | 清理旧表 | ✅ 必要 |
| 009 | update_sso_find_user.sql | 更新SSO查询 | ✅ 必要 |

---

## 二、问题分析

### 🔴 严重问题：废弃表未清理

**废弃表**: `lottery_users`

**问题描述**:
- 001_create_tables.sql 创建了 `lottery_users` 表
- 006_add_sso_support.sql 再次创建（IF NOT EXISTS）
- 但 009_update_sso_find_user.sql 已将 `find_user_by_student_id` 函数改为查询 `lottery_volunteers` 表
- **结果**: `lottery_users` 表现在完全无用，占用空间

**代码验证**:
```sql
-- 001_create_tables.sql (第167-178行) 创建 lottery_users
-- 006_add_sso_support.sql (第9-20行) 再次创建 lottery_users
-- 009_update_sso_find_user.sql (第8-40行) 函数改为查询 lottery_volunteers
```

**Edge Function 验证**:
- `supabase/functions/sso-auth/index.ts` (第143-171行)
- 直接查询 `lottery_volunteers` 表，不再使用 `lottery_users`

---

### 🟡 中等问题：重复定义

**1. find_user_by_student_id 函数定义3次**

| 文件 | 查询的表 | 结果 |
|------|---------|------|
| 003_create_functions.sql (第227-257行) | lottery_users | ❌ 被覆盖 |
| 006_add_sso_support.sql (第93-123行) | lottery_users | ❌ 被覆盖 |
| 009_update_sso_find_user.sql (第8-40行) | lottery_volunteers | ✅ 最终版本 |

**2. update_user_login_time 函数定义2次**

| 文件 | 更新的表 | 状态 |
|------|---------|------|
| 003_create_functions.sql (第262-269行) | lottery_users | ❌ 无用 |
| 006_add_sso_support.sql (第130-137行) | lottery_users | ❌ 无用 |

**原因**: `lottery_volunteers` 表没有 `last_login_at` 字段，该函数已废弃

---

### 🟢 轻微问题：索引重复创建

**lottery_volunteers 索引**:
- 002_create_indexes.sql 创建了基础索引
- 007_add_verify_user_rpc.sql 再次创建相同索引（使用 `IF NOT EXISTS`）
- **结果**: 无实际影响，但代码冗余

---

## 三、与代码逻辑一致性检查

### 3.1 表使用情况

| 表名 | 代码中使用 | 迁移中创建 | 状态 |
|------|-----------|-----------|------|
| lottery_academies | ✅ AppContext.tsx, VolunteerManagePage.tsx | ✅ 001 | 一致 |
| lottery_volunteers | ✅ 所有页面 | ✅ 001 | 一致 |
| lottery_batches | ✅ batchStorage.ts | ✅ 001 | 一致 |
| lottery_exam_rooms | ✅ examRoomStorage.ts | ✅ 001 | 一致 |
| lottery_groups | ✅ groupStorage.ts | ✅ 001 | 一致 |
| lottery_candidates | ✅ candidateStorage.ts | ✅ 001 | 一致 |
| lottery_volunteer_exam_rooms | ✅ volunteerStorage.ts | ✅ 001 | 一致 |
| lottery_group_volunteers | ✅ volunteerStorage.ts | ✅ 001 | 一致 |
| **lottery_users** | ❌ **无** | ✅ 001, 006 | **不一致！** |
| lottery_admins | ❌ 无 | ❌ 008已清理 | 一致 |

### 3.2 函数使用情况

| 函数名 | 代码中使用 | 迁移中定义 | 状态 |
|--------|-----------|-----------|------|
| verify_user | ✅ LoginPage.tsx (supabase.rpc) | ✅ 007 | 一致 |
| find_user_by_student_id | ✅ Edge Function | ✅ 009 | 一致 |
| update_user_login_time | ❌ 无 | ⚠️ 003, 006 | 不一致（无用） |

---

## 四、清理建议

### 4.1 立即清理（高优先级）

创建新的迁移文件 `010_cleanup_unused_objects.sql`:

```sql
-- Migration: 010_cleanup_unused_objects
-- Description: 清理废弃的数据库对象

-- ============================================
-- 1. 删除废弃的 lottery_users 表
-- ============================================
-- 注意：此表已不再使用，SSO 直接查询 lottery_volunteers
DROP TABLE IF EXISTS lottery_users CASCADE;

-- ============================================
-- 2. 删除废弃的函数
-- ============================================
-- update_user_login_time 更新的是 lottery_users 表，已无意义
DROP FUNCTION IF EXISTS update_user_login_time(TEXT);

-- ============================================
-- 3. 删除废弃的触发器（如果存在）
-- ============================================
DROP TRIGGER IF EXISTS trg_update_user_last_login ON lottery_users;
DROP FUNCTION IF EXISTS update_user_last_login();

-- ============================================
-- 4. 清理完成
-- ============================================
NOTIFY pgrst, 'reload schema';
```

### 4.2 可选优化（低优先级）

**合并冗余索引创建**: 将 007 中的索引创建移到 002 中，避免重复检查

**清理注释掉的代码**: 007 中 91-106 行的注释块可以删除

---

## 五、最终推荐迁移文件结构

```
supabase/migrations/
├── 001_create_tables.sql          # 创建核心表（移除 lottery_users）
├── 002_create_indexes.sql         # 创建索引（添加 007 中的索引）
├── 003_create_functions.sql       # 基础函数（移除 SSO 相关）
├── 004_enable_rls.sql             # 行级安全
├── 005_seed_data.sql              # 初始数据
├── 006_add_sso_support.sql        # SSO 支持（简化版）
├── 007_add_verify_user_rpc.sql    # 登录验证函数
├── 008_cleanup_legacy.sql         # 清理旧表
├── 009_update_sso_find_user.sql   # 更新 SSO 查询
└── 010_cleanup_unused_objects.sql # 清理废弃对象（新增）
```

---

## 六、验证命令

部署前执行以下 SQL 验证：

```sql
-- 1. 验证 lottery_users 是否为空（可以安全删除）
SELECT COUNT(*) FROM lottery_users;
-- 预期结果: 0

-- 2. 验证函数查询的表
SELECT prosrc FROM pg_proc WHERE proname = 'find_user_by_student_id';
-- 预期结果: 包含 "lottery_volunteers"

-- 3. 验证所有必要表存在
SELECT tablename FROM pg_tables 
WHERE schemaname = 'public' 
AND tablename LIKE 'lottery_%';
-- 预期结果: 8个表（不含 lottery_users）

-- 4. 验证 verify_user 函数存在
SELECT proname FROM pg_proc WHERE proname = 'verify_user';
-- 预期结果: verify_user
```

---

## 七、总结

| 问题级别 | 数量 | 说明 |
|---------|------|------|
| 🔴 严重 | 1 | lottery_users 表废弃但未清理 |
| 🟡 中等 | 2 | 函数重复定义，存在无用函数 |
| 🟢 轻微 | 1 | 索引重复创建 |

**总体评价**: 迁移文件基本合理，但需要清理 `lottery_users` 表及其相关对象。

**建议操作**:
1. ✅ 立即创建 010_cleanup_unused_objects.sql 清理废弃表
2. ⚠️ 可选：优化索引创建逻辑
3. ⚠️ 可选：删除无用注释代码
