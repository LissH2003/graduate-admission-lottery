# 北科大研究生复试抽签系统 - 部署检查报告

**检查时间**: 2026-03-11  
**检查人**: Kimi Code CLI  
**项目版本**: v2.1.0

---

## 一、Supabase 后端检查

### 1.1 数据库迁移文件检查

| 迁移文件 | 状态 | 说明 |
|---------|------|------|
| `001_create_tables.sql` | ✅ 存在 | 创建 lottery_volunteers, lottery_academies 等表 |
| `002_create_indexes.sql` | ✅ 存在 | 创建索引 |
| `003_create_functions.sql` | ✅ 存在 | 创建基础 RPC 函数 |
| `004_enable_rls.sql` | ✅ 存在 | 启用行级安全策略 |
| `005_seed_data.sql` | ✅ 存在 | 初始数据 |
| `006_add_sso_support.sql` | ✅ 存在 | SSO 支持 |
| `007_add_verify_user_rpc.sql` | ✅ 存在 | 统一登录验证函数 |
| `008_cleanup_legacy.sql` | ✅ 存在 | 清理旧表 |
| `009_update_sso_find_user.sql` | ✅ 存在 | 更新 find_user_by_student_id 函数 |

**检查结果**: 所有迁移文件已准备就绪 ✅

### 1.2 关键函数检查

| 函数名 | 所在文件 | 状态 | 用途 |
|--------|---------|------|------|
| `verify_user` | 007_add_verify_user_rpc.sql | ✅ | 本地测试登录验证 |
| `find_user_by_student_id` | 009_update_sso_find_user.sql | ✅ | SSO 登录查询用户 |

**检查结果**: 关键 RPC 函数已定义 ✅

### 1.3 需要执行的 SQL 检查

部署前请在 Supabase Dashboard SQL Editor 中执行以下检查：

```sql
-- 检查 lottery_volunteers 表是否存在
SELECT EXISTS (
  SELECT FROM information_schema.tables 
  WHERE table_schema = 'public' 
  AND table_name = 'lottery_volunteers'
);
-- 预期结果: true

-- 检查 verify_user 函数是否存在
SELECT EXISTS (
  SELECT FROM pg_proc 
  WHERE proname = 'verify_user'
);
-- 预期结果: true

-- 检查 find_user_by_student_id 函数是否查询 lottery_volunteers
SELECT prosrc FROM pg_proc WHERE proname = 'find_user_by_student_id';
-- 预期结果: 包含 "lottery_volunteers"

-- 检查学院数据
SELECT COUNT(*) FROM lottery_academies;
-- 预期结果: > 0

-- 检查是否有管理员账号
SELECT username, name, role FROM lottery_volunteers WHERE role = 'admin' LIMIT 3;
-- 预期结果: 至少返回一条记录
```

---

## 二、Edge Function 检查

### 2.1 部署状态

| Edge Function | 文件路径 | 状态 |
|--------------|---------|------|
| `sso-auth` | supabase/functions/sso-auth/index.ts | ✅ 代码就绪 |

### 2.2 需要的环境变量

部署 Edge Function 时需要设置以下环境变量：

```bash
# 在 Supabase Dashboard > Edge Functions > 环境变量中设置
SSO_CLIENT_ID=your-client-id-from-zhuyun
SSO_CLIENT_SECRET=your-client-secret-from-zhuyun
SSO_CALLBACK_URL=http://your-domain/auth/callback
```

**⚠️ 重要**: Edge Function 的环境变量与前端环境变量是独立的！

### 2.3 部署命令

```bash
# 部署 Edge Function
supabase functions deploy sso-auth

# 设置环境变量
supabase secrets set SSO_CLIENT_ID=your-client-id
supabase secrets set SSO_CLIENT_SECRET=your-client-secret
supabase secrets set SSO_CALLBACK_URL=http://your-domain/auth/callback
```

---

## 三、前端环境配置检查

### 3.1 配置文件检查

| 配置文件 | 状态 | 说明 |
|---------|------|------|
| `.env` | ✅ | 开发环境配置 |
| `.env.production` | ⚠️ | 需要修改占位符 |
| `.env.local` | ✅ | 本地覆盖配置 |

### 3.2 .env.production 修改清单

**当前配置**（需要修改）:

```bash
VITE_AUTH_MODE=sso                                    ✅ 正确
VITE_SSO_CLIENT_ID=your-client-id-from-zhuyun        ❌ 需要修改为真实 client_id
VITE_SSO_CALLBACK_URL=http://DEPLOY_IP/auth/callback ❌ 需要修改为真实域名
VITE_EDGE_FUNCTION_URL=https://bmvjhgvjdpmfycptlvla.supabase.co/functions/v1 ✅ 正确
```

### 3.3 必须修改的配置项

| 配置项 | 当前值 | 需要修改为 | 获取方式 |
|--------|--------|-----------|---------|
| `VITE_SSO_CLIENT_ID` | `your-client-id-from-zhuyun` | 学校分配的 client_id | 联系信息中心 |
| `VITE_SSO_CALLBACK_URL` | `http://DEPLOY_IP/auth/callback` | `http://实际IP/auth/callback` | 部署服务器内网IP |

---

## 四、竹云 SSO 配置检查

### 4.1 需要向学校信息中心申请

| 项目 | 状态 | 说明 |
|------|------|------|
| client_id | ❌ 待申请 | 竹云 OAuth2 客户端ID |
| client_secret | ❌ 待申请 | 竹云 OAuth2 客户端密钥 |
| 回调地址注册 | ❌ 待注册 | 需要在竹云平台注册回调地址 |

### 4.2 竹云平台配置检查清单

- [ ] 应用已创建并启用
- [ ] 授权模式：authorization_code
- [ ] 回调地址白名单已添加：`http://你的IP/auth/callback`
- [ ] 已获取 client_id 和 client_secret

---

## 五、前端构建检查

### 5.1 构建前检查

```bash
# 1. 切换到生产分支/标签
git checkout main

# 2. 确保使用生产环境变量
cp .env.production .env

# 3. 安装依赖
npm ci

# 4. 执行构建
npm run build
```

### 5.2 构建产物检查

构建成功后检查：

| 检查项 | 路径 | 预期结果 |
|--------|------|---------|
| index.html | dist/index.html | 存在 |
| JS 文件 | dist/assets/*.js | 存在且压缩 |
| CSS 文件 | dist/assets/*.css | 存在且压缩 |

---

## 六、关键问题与风险

### 🔴 高风险问题

| 问题 | 影响 | 解决方案 |
|------|------|---------|
| client_id 未配置 | SSO 登录跳转失败 | 联系信息中心申请 |
| 回调地址不匹配 | SSO 登录后返回失败 | 确认竹云配置与前端配置一致 |

### 🟡 中风险问题

| 问题 | 影响 | 解决方案 |
|------|------|---------|
| Edge Function 未部署 | SSO 登录后无法处理回调 | 执行部署命令 |
| 数据库未初始化 | 登录验证失败 | 执行所有迁移文件 |

### 🟢 低风险问题

| 问题 | 影响 | 解决方案 |
|------|------|---------|
| 测试账号未清理 | 生产环境有测试数据 | 删除或禁用测试账号 |

---

## 七、部署验证步骤

### 7.1 部署后验证清单

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 访问登录页 | 显示统一身份认证登录按钮 |
| 2 | 点击 SSO 登录 | 跳转到竹云登录页 |
| 3 | 使用学校账号登录 | 成功返回系统 |
| 4 | 管理员登录 | 跳转到 /admin-home |
| 5 | 志愿者登录 | 跳转到 /volunteer-exam-select |
| 6 | 退出登录 | 清除状态，返回登录页 |

### 7.2 浏览器控制台检查

打开浏览器开发者工具，检查：

```javascript
// 检查 Supabase 连接
console.log('Supabase connected:', !!supabase);

// 检查登录状态
console.log('Auth state:', localStorage.getItem('lottery_auth_user'));
```

---

## 八、修改建议

### 立即需要修改的文件

1. **frontend/.env.production**
   - 第11行: `VITE_SSO_CLIENT_ID=your-client-id-from-zhuyun` → 改为真实值
   - 第15行: `VITE_SSO_CALLBACK_URL=http://DEPLOY_IP/auth/callback` → 改为真实IP

2. **Supabase Edge Function 环境变量**
   - 设置 `SSO_CLIENT_ID`
   - 设置 `SSO_CLIENT_SECRET`
   - 设置 `SSO_CALLBACK_URL`

---

## 九、总结

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 代码完整性 | ✅ 通过 | 所有文件已就绪 |
| 数据库迁移 | ⚠️ 待执行 | 需在生产环境执行 SQL |
| Edge Function | ⚠️ 待部署 | 需部署并配置环境变量 |
| 前端配置 | ❌ 需修改 | client_id 和回调地址为占位符 |
| 竹云配置 | ❌ 待申请 | 需联系信息中心 |

**整体状态**: ⚠️ **条件性通过** - 需要完成上述修改后才能部署

---

## 十、联系信息

**部署支持**:
- 竹云 OAuth2 申请：学校信息中心
- Supabase 问题：查看 Supabase Dashboard 日志
- 前端问题：检查浏览器控制台 Network 和 Console 标签

**报告生成时间**: 2026-03-11 09:15:00
