
# ================================================================================
# Nginx 主配置文件 - 北科大服务器
# 路径: /opt/ai-nginx/nginx/conf.d/index.conf
# 说明: 整服务器配置，包含多个项目
# ================================================================================


# 抽签系统 - HTTP (80端口)
server {
    listen       80;
    server_name  localhost;

    # 前端静态文件 - JS/CSS 资源（优先匹配，避免被 try_files 重定向）
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|otf)$ {
        root /opt/ai-nginx/nginx/home/lottery;
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # 前端静态文件
    location / {
        root /opt/ai-nginx/nginx/home/lottery;
        index index.html;
        try_files $uri $uri/ /index.html;

        add_header Access-Control-Allow-Methods 'GET,POST,OPTIONS';
        add_header Access-Control-Allow-Credentials true;
        add_header X-Frame-Options SAMEORIGIN;
    }

    # Edge Function 代理（SSO回调等）
    location /functions/v1/ {
        proxy_pass http://localhost:8000/functions/v1/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # 错误页面
    error_page 404 /404.html;
    location = /40x.html {
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
    }
}

# 抽签系统 - HTTPS (443端口) 
# server {
#     listen       443 ssl;
#     server_name  localhost;
#
#     # SSL 证书配置（需替换为实际路径）
#     ssl_certificate     /opt/nginx/sslcert/lottery.pem;
#     ssl_certificate_key /opt/nginx/sslcert/lottery.key;
#     ssl_session_timeout 5m;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         root /opt/ai-nginx/nginx/home/lottery;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#
#         add_header Access-Control-Allow-Methods 'GET,POST,OPTIONS';
#         add_header Access-Control-Allow-Credentials true;
#         add_header X-Frame-Options SAMEORIGIN;
#         add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     }
#
#     location /functions/v1/ {
#         proxy_pass http://localhost:8000/functions/v1/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_connect_timeout 30s;
#         proxy_send_timeout 30s;
#         proxy_read_timeout 30s;
#     }
#
#     error_page 404 /404.html;
#     location = /40x.html {
#     }
#
#     error_page 500 502 503 504 /50x.html;
#     location = /50x.html {
#     }
# }


# ========================================
# 项目2: 后台管理系统 (端口81) - 【如需启用，取消注释】
# ========================================
# server {
#     listen       81;
#     server_name  localhost;
#     charset utf-8;
#
#     location / {
#         root /opt/nginx/home/admin;
#         index index.html;
#         try_files $uri $uri/ @router;
#     }
#
#     location @router {
#         rewrite ^.*$ /index.html last;
#     }
#
#     location /api {
#         proxy_pass http://127.0.0.1:8080/;
#         add_header Cache-Control no-store;
#     }
# }


# ========================================
# 项目3: 预留 - 【请在此处添加新项目】
# ========================================
#
# 模板:
# server {
#     listen       82;
#     server_name  localhost;
#     
#     location / {
#         root /opt/nginx/home/新项目目录;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }
# }
#


# ========================================
# 全局 HTTPS 跳转（可选）
# ========================================
# 如需强制 HTTPS，取消下方注释
#
# server {
#     listen       80;
#     server_name  localhost;
#     rewrite ^(.*)$ https://$host$1 permanent;
# }


# ========================================
# 保留备份配置（参考用）
# ========================================
#
# 【以下为原配置文件保留的备份，如需恢复可参考】
#
# 原 - 后台 web (端口81):
# server {
#     listen       81;
#     server_name  localhost;
#     charset utf-8;
#     location / {
#         root /opt/nginx/home/admin;
#         index index.html;
#         try_files $uri $uri/ @router;
#     }
#     location @router {
#         rewrite ^.*$ /index.html last;
#     }
#     location /api {
#         proxy_pass http://127.0.0.1:8080/;
#         add_header Cache-Control no-store;
#     }
# }
#
# 【其他原配置已整合或注释，详见上方】

