# 部署修改清单

**文档用途**: 生产环境部署前逐项检查  
**最后更新**: 2026-03-11

---

## 一、必须修改的文件

### 1.1 前端配置文件

#### 📄 `frontend/.env.production`

```bash
# ============================================
# 第 1-2 项：必须修改（联系学校信息中心获取）
# ============================================

# 第 11 行：竹云 OAuth client_id
# 原值：
VITE_SSO_CLIENT_ID=your-client-id-from-zhuyun
# 改为：
VITE_SSO_CLIENT_ID=YW2025032  # 示例，替换为真实值

# 第 15 行：回调地址（你的服务器IP/域名）
# 原值：
VITE_SSO_CALLBACK_URL=http://DEPLOY_IP/auth/callback
# 改为：
VITE_SSO_CALLBACK_URL=http://10.0.0.1/auth/callback  # 示例，替换为真实IP

# ============================================
# 第 3 项：确认以下配置正确（通常无需修改）
# ============================================

# 第 7 行：Supabase 项目URL
VITE_SUPABASE_URL=https://bmvjhgvjdpmfycptlvla.supabase.co

# 第 8 行：匿名密钥
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 第 18 行：Edge Function URL
VITE_EDGE_FUNCTION_URL=https://bmvjhgvjdpmfycptlvla.supabase.co/functions/v1
```

**修改后操作**:
```bash
cd frontend
cp .env.production .env
npm ci
npm run build
# 部署 dist/ 目录到服务器
```

---

## 二、Supabase Dashboard 配置

### 2.1 Edge Function Secrets（必须设置）

**设置路径**: Supabase Dashboard → Edge Functions → Secrets

| 变量名 | 值 | 获取方式 |
|--------|-----|---------|
| `SSO_CLIENT_ID` | `YW2025032` | 学校信息中心 |
| `SSO_CLIENT_SECRET` | `your-secret` | 学校信息中心 |
| `SSO_CALLBACK_URL` | `http://10.0.0.1/auth/callback` | 你的服务器IP |

**设置命令**:
```bash
supabase secrets set SSO_CLIENT_ID=your-client-id
supabase secrets set SSO_CLIENT_SECRET=your-client-secret
supabase secrets set SSO_CALLBACK_URL=http://your-domain/auth/callback
```

### 2.2 数据库初始化（SQL Editor）

**执行路径**: Supabase Dashboard → SQL Editor → New query

**执行文件**: `supabase/migrations/000_init_database.sql`

```sql
-- 复制 000_init_database.sql 的全部内容
-- 粘贴到 SQL Editor
-- 点击 Run
```

**验证命令**:
```sql
-- 检查表数量（应返回 8）
SELECT COUNT(*) FROM information_schema.tables 
WHERE table_schema = 'public' AND table_name LIKE 'lottery_%';

-- 检查初始账号（应返回 3 条记录）
SELECT username, name, role FROM lottery_volunteers;
```

---

## 三、竹云平台配置（学校信息中心）

### 3.1 必须注册的信息

联系学校信息中心，在竹云 OAuth 平台注册：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 回调地址 | `http://your-domain/auth/callback` | 必须与代码中一致 |
| 授权模式 | `authorization_code` | 授权码模式 |
| Client ID | `YW2025032` | 分配给应用的ID |
| Client Secret | `xxxxxxxx` | 密钥（保密） |

**注意**：回调地址必须与以下三个地方完全一致：
1. 前端 `.env.production` 中的 `VITE_SSO_CALLBACK_URL`
2. Edge Function Secrets 中的 `SSO_CALLBACK_URL`
3. 竹云平台注册的回调地址

---

## 四、服务器部署配置

### 4.1 Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 前端静态文件
    location / {
        root /var/www/lottery-frontend/dist;
        index index.html;
        try_files $uri $uri/ /index.html;  # SPA 路由支持
    }
    
    # 可选：API 代理（如需要）
    # location /api {
    #     proxy_pass https://bmvjhgvjdpmfycptlvla.supabase.co;
    # }
}
```

### 4.2 需要上传的文件

```
服务器目录结构：
/var/www/lottery-frontend/
└── dist/                    # 前端构建产物
    ├── index.html
    └── assets/
        ├── index-xxx.js
        └── index-xxx.css
```

---

## 五、部署命令汇总

### 5.1 数据库初始化
```bash
# 方法1：SQL Editor 手动执行
# 复制 000_init_database.sql 到 Supabase SQL Editor 执行

# 方法2：CLI（如已配置）
supabase db reset
```

### 5.2 Edge Function 部署
```bash
# 部署
cd supabase/functions/sso-auth
supabase functions deploy sso-auth

# 设置环境变量
supabase secrets set SSO_CLIENT_ID=your-client-id
supabase secrets set SSO_CLIENT_SECRET=your-client-secret
supabase secrets set SSO_CALLBACK_URL=http://your-domain/auth/callback

# 验证
supabase functions list
```

### 5.3 前端部署
```bash
cd frontend

# 复制生产配置
cp .env.production .env

# 安装依赖
npm ci

# 构建
npm run build

# 部署 dist/ 到服务器
scp -r dist/* user@server:/var/www/lottery-frontend/
```

---

## 六、部署验证清单

### 6.1 基础功能验证

| 验证项 | 操作 | 预期结果 |
|--------|------|---------|
| 数据库连接 | 访问登录页 | 页面正常加载 |
| 表结构 | 执行 SQL 检查 | 8 个 lottery 表存在 |
| 初始账号 | 用 admin/admin123 登录 | 登录成功 |

### 6.2 SSO 登录验证

| 验证项 | 操作 | 预期结果 |
|--------|------|---------|
| SSO 跳转 | 点击"统一身份认证登录" | 跳转到竹云页面 |
| 竹云登录 | 输入学校账号密码 | 成功回调到系统 |
| 角色判断 | 管理员登录 | 跳转到 /admin-home |
| 角色判断 | 志愿者登录 | 跳转到 /volunteer-exam-select |

### 6.3 验证命令

```bash
# 检查 Edge Function 运行状态
curl -X POST https://bmvjhgvjdpmfycptlvla.supabase.co/functions/v1/sso-auth \
  -H "Content-Type: application/json" \
  -d '{"code":"test","state":"12345678"}'
# 预期返回：{"error":"Authentication failed",...} （表示服务运行正常）

# 检查数据库连接
# 在浏览器控制台执行：
await supabase.from('lottery_volunteers').select('count')
# 预期返回：数据正常
```

---

## 七、常见问题排查

### 7.1 登录跳转失败

**现象**: 点击 SSO 登录报错 "client_id 为空"

**检查**:
1. 确认 `.env.production` 中 `VITE_SSO_CLIENT_ID` 已修改
2. 确认构建时使用了 `.env.production`
3. 确认前端代码中 `redirectToSSO()` 函数读取了环境变量

### 7.2 回调后报错

**现象**: 竹云登录后返回报错

**检查**:
1. Edge Function Secrets 中 `SSO_CLIENT_ID` 和 `SSO_CLIENT_SECRET` 是否设置
2. 三个回调地址是否完全一致（前端、Edge Function、竹云平台）
3. 查看 Edge Function 日志：Supabase Dashboard → Edge Functions → sso-auth → Logs

### 7.3 用户不存在

**现象**: 提示"账号不存在"

**检查**:
1. `lottery_volunteers` 表中是否有该用户的 `login_id`
2. 用户状态是否为 `active`

---

## 八、联系支持

| 问题类型 | 联系方式 |
|---------|---------|
| 竹云 OAuth 配置 | 学校信息中心 |
| Supabase 技术问题 | Supabase 官方文档 |
| 系统功能问题 | 开发团队 |

---

## 九、检查清单（打印版）

部署前逐项打勾：

- [√] `frontend/.env.production` 中 `VITE_SSO_CLIENT_ID` 已修改
- [√] `frontend/.env.production` 中 `VITE_SSO_CALLBACK_URL` 已修改
- [√]数据库迁移 `000_init_database.sql` 已执行
- [√] Edge Function `sso-auth` 已部署
- [ ] Edge Function Secrets 已设置（SSO_CLIENT_ID, SSO_CLIENT_SECRET, SSO_CALLBACK_URL）
- [ ] 竹云平台回调地址已注册
- [ ] 前端代码已构建（`npm run build`）
- [ ] 前端代码已上传到服务器
- [ ] Nginx 配置已更新（如使用）
- [ ] 基础登录测试通过（admin/admin123）
- [ ] SSO 登录测试通过

**全部勾选完成后，系统可正式使用。**
