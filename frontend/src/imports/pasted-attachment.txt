请基于我上传的《研究生复试抽签系统 - 高保真原型设计需求》文档，按以下流程执行：

## 【第一步】建立设计系统（先执行，不画页面）
基于文档第2章"设计系统规范"，创建以下全局样式（Styles）和组件（Components）：

**颜色样式**：
- Primary/500: #1E40AF, Primary/400: #3B82F6
- Success: #059669, Warning: #D97706, Error: #DC2626, Info: #0891B2
- Neutral/900: #111827（标题）, /600: #4B5563（正文）, /400: #9CA3AF（辅助）
- Background: #F9FAFB, Surface: #FFFFFF, Divider: #E5E7EB

**字体样式**（PingFang SC）：
- Display/144px（抽签结果数字，投影专用）
- H1/48px（副屏标题）, H2/36px（时钟）, H3/24px（页面标题）
- Body/16px, Caption/14px, Small/12px

**母版组件**（带Variants状态）：
- Button/Primary（Default/Hover/Loading/Disabled，高度48px，圆角8px）
- Button/Danger（标记缺考专用，红色#DC2626）
- Card/Candidate（考生卡片，72px高，圆角8px，含选中态左边框4px）
- Tag/Status（四种：amber待抽签/green已抽签/red缺考/blue进行中）
- Input/Text（含Focus状态，border #3B82F6）
- Modal/Base（圆角12px，阴影0 20px 25px rgba(0,0,0,0.15)）

完成后暂停，等待我确认设计系统正确再继续。

---

## 【第二步】按优先级分批次生成页面（严格按序）

### Batch 1 - P0 核心演示页（必须先生成）
**页面V2-PC抽签控制台**（文档4.2.2节）：
- 画布：1440×900px，背景#F9FAFB
- 布局：顶部栏80px固定 + 三栏主体（左25% min-320px/中50%/右25% min-280px）
- 左栏（背景#F3F4F6）：
  - Tab：待抽签(23)/已抽签(12)/缺考(0)，选中下划线2px #3B82F6
  - 考生卡片：白色，72px高，姓名18px（首字**脱敏），身份证后四位14px灰色
  - 选中态：左边框4px #3B82F6，背景#EFF6FF
- 中栏：
  - 考生信息区：姓名48px居中，考生号24px灰色（2024****0001格式）
  - 身份验证：输入框+验证按钮，通过后显示绿色对勾
  - **核心抽签按钮**：圆形280px直径，渐变蓝(#1E40AF→#3B82F6)，文字"点击抽签"24px白色
  - **抽签结果**：覆盖显示超大数字"05"（144px，#059669，弹簧动画scale 0.8→1.0）
  - 最近记录条：底部固定，横向滚动，显示最近5条
- 右栏：
  - 统计数字：已抽签72px绿色，剩余人数72px（≤5时红色#DC2626并闪烁），缺考72px灰色
  - 环形进度条：SVG，直径120px，中央显示百分比
  - 操作按钮：标记缺考（Danger，全宽56px高），Grid 2x2次要按钮

**关键约束**：
- 所有投影专用数字（签号144px、时钟36px）必须使用等宽字体Monaco
- 严格按25%/50%/25%比例，使用Auto Layout约束
- 颜色严格使用文档色值，禁止自行调整

### Batch 2 - P1 投屏专用
**页面V3-叫号副屏**（文档4.2.3节）：
- 画布：1920×1080px，背景纯黑#000000
- 上半屏（50%）："请考生入场"48px灰色(#9CA3AF) + 签号"05"240px青色(#06B6D4，加text-shadow发光) + 姓名72px白色
- 下半屏（50%）："请准备"24px灰色 + 下一位签号+姓名48px白色（透明度0.8）
- 侧边：当前时间32px等宽字体，进度条（青色填充）

### Batch 3 - P2 管理端（简化版）
**页面M4-考场配置**（文档4.1.4节，砍掉Step3权限分配）：
- 顶部：简化导航（无左侧边栏，改为顶部Tab）
- 筛选栏：学院Select + 日期RangePicker + 搜索框 + 新建按钮（Primary）
- 表格：列=考场名称/学院/时间/地点/进度条/状态Tag/操作
- 状态Tag：未开始(灰)/进行中(蓝，带闪烁圆点)/已结束(绿)
- 新建Modal：仅2步（基础信息+上传Excel），去掉Step3权限分配

**页面M2-学院管理**（文档4.1.2节，**砍掉树形结构**，改为Tab卡片）：
- 学院选择器：横向Tab卡片（机械工程学院/电子信息学院...），选中态边框2px #3B82F6
- 详情卡片：统计行（总人数/管理员/志愿者/考场），人员列表表格
- 角色Tag：超管红底/志愿者绿底，简单下拉选择（**不做权限矩阵**）

**页面-登录页**（文档4.1.1节简化）：
- 居中卡片480px宽，角色选择（超管/志愿者单选卡片），工号密码输入，登录按钮

---

## 【第三步】原型连线（可点击演示）
完成所有页面后，添加Prototype交互：
1. 登录页 → 考场选择（点击"登录"）
2. 考场卡片 → V2控制台（点击"进入抽签"）
3. V2控制台 → V3副屏（点击"投屏显示"）
4. V2标记缺考 → 确认Modal → 返回更新列表

---

## 【关键裁剪确认】（8-10天交付版）
以下功能已砍，**不要生成**：
❌ Slot Machine动画（改为简单CSS转圈）
❌ 树形组织架构（改为Tab卡片）
❌ 角色权限矩阵M3（改为简单Tag）
❌ 独立移动端H5（PC响应式适配即可）
❌ 扫码功能P3（改为手输身份证后4位）
❌ 操作日志前端展示（仅后端记录）

**投影适配检查点**（生成后我必须确认）：
- [ ] V2页面签号144px在150%缩放下仍清晰
- [ ] V3页面青色#06B6D4在黑色背景对比度&gt;7:1
- [ ] 所有按钮触摸目标≥44×44px

请确认理解上述流程，回复"已理解，等待生成设计系统"，然后开始执行。